[tool.poetry]
name = "cryptodataset"
version = "0.1.0"
description = ""
authors = ["なるみ"]

[tool.poetry.dependencies]
python = "^3.8"
ccxt = "^1.90.10"
pandas = "^1.4.3"
loguru = "^0.6.0"
click = "^8.1.3"

[tool.poetry.dev-dependencies]
yapf = "^0.32.0"
pytest = "^7.1.2"
isort = "^5.10.1"
flake8 = "^4.0.1"

[tool.poetry.scripts]
cryptodata = "cryptodata.cli:cli"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
